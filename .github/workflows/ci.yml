name: CI

on:
  push:
    branches: [main]
    paths-ignore: [docs/**]
  pull_request:
    branches: [main]
    paths-ignore: [docs/**]

jobs:
  ci:
    uses: 1mb-dev/.github/.github/workflows/ci-go.yml@main
    with:
      go-versions: '["1.25", "1.26"]'
